name: CI checks

on:
  pull_request:
    paths:
      - '.github/workflows/**'

jobs:
  ci-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 16
      - run: npm install
      - run: npm run test:ci
